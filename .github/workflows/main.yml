name: vps-max-parallel

on:
  workflow_dispatch:

concurrency:
  group: vps-max
  cancel-in-progress: false

jobs:
  vps1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps3:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps4:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps5:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps6:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps7:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps8:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps9:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py

  vps10:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          npm install gradient-string@1.2.0 cheerio axios colors puppeteer-real-browser
          python3 bot.py
